export class GlobalConstants {
    // Mensagem
    public static genericError:string = 'Algo deu errado. Tente novamente mais tarde';
    
    public static unauthroized: string = 'Você não tem acesso para acessar essa pagina';

    public static productExistError:string = 'Produto já existe';

    public static productAdded:string = 'Produto adicionado corretamente';
    
    
    
    
    // Regex
    public static nameRegex:string = '[a-zA-Z0-9 ]*';

    public static emailRegex:string = '[A-Za-z0-9._%-]+@[A-Za-z0-9._%-]+\\.[a-z]{2,3}';

    public static contactRegex:string = '^[e0-9]{10,10}$';

    // Vars
    public static error:string = 'error';



}